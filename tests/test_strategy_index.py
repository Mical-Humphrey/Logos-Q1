from __future__ import annotations

import pytest


pytest.skip(
    "Strategy index artifacts are maintained by Agent1; consumer coverage will activate once v1 is published.",
    allow_module_level=True,
)
